<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.caio.ibapa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BulletinsListActivity" />
        <activity android:name=".BulletinsMenuActivity" />
        <activity android:name=".EscalaServicoActivity" />
        <activity android:name=".SobreActivity" />
        <activity android:name=".OfertaActivity" />
        <activity
            android:name=".EditorialActivity"
            android:label="@string/title_activity_editorial"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".AgendaSemanaActivity"></activity>
    </application>

</manifest>
